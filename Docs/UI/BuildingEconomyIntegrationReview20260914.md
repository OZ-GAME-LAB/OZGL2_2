# 건물·재화 UI 연결 검토 및 공유 준비 — 2026-09-14

## 작업 기준

- 프로젝트: `D:/Documents/GitHub/OZGL2_2`
- 작업 브랜치: `feature/ui-building-phase-binding`
- 시작 HEAD 및 원격 브랜치: `e777134841945caf8a1224f88474760b080cb002`
- 조회한 `origin/dev`: `3d5da92` (fetch만 수행, 전체 dev를 merge/pull하지 않음)
- Unity: `6000.3.23f1`

현재까지 완료한 UI를 공유 가능한 커밋으로 정리한다. 최신 dev의 게임플레이를 통합 완료했다는 의미는 아니다. 이 검토에서 팀원 메서드나 공용 씬/에셋을 수정하지 않는다.

## 기존 작업과 의존 코드의 소유 범위

- 현재 워킹 트리에는 이전 UI 작업과, 9월 10일 사용자 승인으로 가져온 코어/웨이브 원본이 함께 미커밋 상태로 남아 있었다.
- `Assets/Scripts/Core`, `Assets/Data/Waves`의 65개 파일을 원본 커밋 `2c088a650831c909442af866089e6070b73ae9ed`와 Git blob 기준으로 비교했다. 불일치 0개.
- 이 코어 의존성을 빼고 UI만 공유하면 브랜치의 기존 HEAD에는 새 단계 enum/API가 없어 같은 상태를 재현할 수 없다. 원본 의존성 반영과 UI 작업을 구분하여 기록한다.
- `Assets/Art/Fonts/NotoSansKR/NotoSansKR SDF.asset`의 기존 사용자 변경은 보존하고 커밋에서 제외한다.
- ProjectSettings, Packages, 팀원 유닛/재화/건물 소스, 공용 게임 씬은 새로 수정하거나 가져오지 않는다.
- 새 테스트 씬은 Build Settings에 추가하지 않는다. 최종 게임 씬이 아니다.

## 실제 건물·재화 연결을 바로 진행할 수 없는 이유

아래 내용은 `origin/dev` 소스의 정적 검토 결과다. 최신 dev를 Unity에서 실행한 결과가 아니다.

| 연결 지점 | 현재 소스에서 확인한 상태 | 필요한 협의/후속 작업 |
| --- | --- | --- |
| 골드 소유자 | `OZGL.KDH.BuildingBuildController`는 `Temp/PlayerWallet`을 참조한다. 기존 HUD는 `RunCurrencyManager`의 골드를 표시한다. | 건물 비용·환급과 HUD가 같은 정식 재화 관리자를 사용하도록 건물/재화 담당자가 연결 계약 확정 |
| 정식 재화 API | dev는 `CanSpend(CurrencyType, int)`, `TrySpend(CurrencyType, int)`, `TryAdd(CurrencyType, int)`, `Initialize(WaveController)`를 사용한다. 로컬 샘플은 기존 `CurrencyAmount` 및 `TryInitialize` 계약을 사용한다. | 최신 팀원 원본을 가져오는 범위를 정한 후 UI 샘플/검사를 새 API에 맞춤. 팀원 API를 예전 모양으로 수정하지 않음 |
| 업그레이드 | 건설/철거 및 점유 건물 교체 경로는 있으나 독립적인 업그레이드 API/전직 선택 계약은 확인되지 않았다. | 일반 교체를 업그레이드로 간주하지 않고 담당자와 비용·전직·결과를 확정 |
| 잠긴 공간 | 슬롯에서 점유 상태는 조회할 수 있지만 미개방/해금 상태의 공개 계약은 확인되지 않았다. | 미개방 공간의 선택·표시·건설 제한 API 협의 |
| 건설 단계 권한 | 건물 컨트롤러의 공개 실행 경로에서 `CanEnterBuildMode` 재검사는 확인되지 않았다. | UI 잠금 외에 실제 실행 주체가 단계/대상/비용을 다시 검사 |
| 실패 시 부분 적용 | `TryBuild`는 재화 차감 후 생성/점유를 수행하며, 그 뒤 실패하는 경로의 환급 코드는 보이지 않는다. | 실패 시 건물과 재화의 부분 적용이 남지 않는지 담당자가 검증. UI에서 임의 보정하지 않음 |
| 환급 견적 | 컨트롤러 기본 `refundRate = 1f`; 기획 문서의 70%는 임시 수치다. | 확정된 시스템 견적을 UI에 전달. UI에서 70%/100% 중 하나를 임의 선택하지 않음 |
| 복수 재화 | 건물 비용은 재화 배열이며 UI의 현재 제안 견적은 골드 단일 값이다. | 골드+보석 비용의 표시 계약 및 일괄 거래 성공/실패 계약 협의 |

`PlayerWallet`과 `RunCurrencyManager`의 잔액을 UI에서 서로 복사하거나, UI가 직접 건물 생성과 비용 차감을 따로 수행하는 우회 구현은 하지 않는다.

## 담당자에게 전달할 내용

> 건설 UI의 단계별 잠금과 요청/응답 처리는 준비되어 있습니다. 실제 연결을 위해 건물 비용·환급을 RunCurrencyManager로 통일하는 API, 업그레이드/해금 상태와 견적 조회, 실패 시 부분 적용 없는 실행 결과를 확인 부탁드립니다. 확정된 API를 기준으로 UI 어댑터에서 연결하겠습니다.

이 문구는 전달용 초안이다. 팀원에게 메시지를 보내거나 GitHub 이슈에 자동 게시하지 않았다.

## 검증 및 게시 결과

Unity Editor 숨김 배치로 현재 워킹 트리의 기존 UI 검사를 재실행했다. 6개 실행 모두 종료 코드 0이다.

| 진입점 (`Game.UI.Editor.MvpRuntimeHudValidation`) | 자체 검사 | 결과 |
| --- | ---: | --- |
| `RunBuildingPhase` | 404 | PASS |
| `Run` (코어/재화 HUD + 분기 + 유닛 정보) | 252 + 85 + 83 = 420 | PASS |
| `RunUnitCounts` | 135 + 58 = 193 | PASS |
| `RunUnitSelection` | 124 | PASS |
| `RunArtifacts` | 1193 | PASS |
| `RunRegression` (HUD/건물 정보/행동/유닛 정보) | 98 + 113 + 150 + 157 = 518 | PASS |
| 합계 | **2852** | **PASS** |

- NUnit 테스트 케이스 수가 아닌 프로젝트 자체 검증문 개수다. 시나리오가 공유하는 검사도 있으므로 모두 독립된 게임 기능 2852개를 검증했다는 의미가 아니다.
- 로그: `Logs/UiPublish20260914/`의 `building-phase.log`, `runtime.log`, `unit-counts.log`, `unit-selection.log`, `artifacts.log`, `editor.log`.
- UI 내부 포인터/레이캐스트 및 실제 로컬 코어/유닛/재화 API를 사용한 검사다. 건물 견적과 아티팩트 데이터는 모의 입력이다.
- 검사 중 Error/Exception이나 C# 오류로 실패한 항목 없음. 기존 팀원 코어의 CS1998 경고는 수정하지 않았다.
- 최신 dev 통합, Player 빌드, 정식 게임 씬 전체 연결은 이번 검증 범위가 아니다.
- 런타임 소스/씬을 추가 수정하지 않고 이전 작업을 검증·공유 준비한 것이다. 오늘 신규 건물 생성/재화 연동을 구현한 것으로 보고하지 않는다.
- UI 변경/관련 데이터·씬·문서 80개와, 이미 승인받아 가져온 원본 의존성 변경 53개를 공유 대상으로 분리했다. 공용 폰트 변경 1개는 제외했다.
- PR, main/dev 머지, GitHub 이슈 자동 갱신은 수행하지 않는다.

## 다음 순서

1. 검증된 기존 UI와 승인된 원본 의존성을 커밋하고 현재 기능 브랜치에 업로드.
2. 최신 dev 원본을 가져올 범위와 위 건물/재화 API를 확정.
3. 팀원 원본을 그대로 사용하고 우리 UI 바인딩/샘플/검사만 새 API에 맞춤.
4. 실제 건물 생성·골드 차감·환급·건설 잠금의 통합 검사 후 dev 대상 PR 준비. PR 머지는 별도 팀 절차로 진행.
