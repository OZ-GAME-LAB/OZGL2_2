# 건설 UI 실제 연동 — 2026-09-15

## 작업 위치와 원본

- 프로젝트: `D:/Documents/GitHub/OZGL2_2`
- 브랜치: `feature/ui-building-integration-20260915` (이전 UI 작업과 미커밋 변경을 그대로 보존한 새 로컬 브랜치)
- 기준 HEAD: `aed54244b180588fe72b8bf0813f29c984350c92`
- 승인받아 반영한 dev 원본: `f948eac46d10377d616861dc765d782676553877`
- 팀원 건물·유닛 등의 원본 로직을 임의 수정하지 않았다. 의존성 226개 파일을 최신 원본으로 반영했고, 바이트 비교에서 차이가 없다.
- dev에서 이동/대체된 옛 유닛 파일 12개(코드와 메타)는 중복 타입/GUID를 피하도록 로그 백업으로 옮겼다. 복구 위치: `Logs/UiBuildingIntegration20260915/BeforeDependencySync/`.
- 기존 공유 폰트의 미커밋 변경, 기존 씬·프리팹·팀 데이터·프로젝트 설정을 보존했다. 전체 dev merge, commit, push, PR, merge는 하지 않았다. PR 대상은 dev다.

## 완료 범위

1. `BuildingSlot.CollectCandidates`와 `BuildingData`에서 실제 후보/가격을 읽어 작은 건설 카탈로그에 표시한다.
2. 빈 슬롯 → 후보 선택 → 건설 요청 → 원본 `BuildingBuildController.TryBuild` 호출 → 실제 건물 생성/슬롯 점유/재화 차감까지 연결했다.
3. 점유 슬롯 선택 → 정보/해체 표시 → 원본 `TryDemolish` 호출 → 실제 점유 해제/오브젝트 제거/환급까지 연결했다.
4. 골드와 보석을 함께 표시하며, 기존 골드 전용 생성자와 요청 계약은 유지했다. UI는 재화를 직접 차감하거나 환급하지 않는다.
5. 재화 변경에 따른 버튼 상태, 중복 클릭, 처리 중 재진입, 선택 후 슬롯/후보/페이즈 변경, 비활성화/재활성화를 방어한다.
6. 최신 유닛의 초기화/피해/스탯 보정 API에 맞춰 우리 UI 샘플·검증 코드만 수정했다. 체력 0은 HpChanged로 표시하며 제거된 Life.Died 이벤트에 의존하지 않는다.

환급 표시 어댑터는 현재 공개된 `BuildCost`와 `RefundRate`를 원본 `RefundCosts`와 동일하게 읽어 표시한다. 환급율을 70%로 임의 확정하지 않았다. 새 검증 씬은 원본 컨트롤러의 기본값 100%를 사용한다. 추후 건물 담당자가 공식 환급 견적 API를 제공하면 그 조회로 교체해야 한다.

## 확인할 씬

`Assets/Scenes/UI/PlayerBuildingIntegration.unity`

- 기존 `PlayerUI.unity`는 보존했다. 새 씬은 UI가 소유하는 별도 연동 검증 씬이다.
- Play → 건설 또는 `+` 슬롯 클릭 → 골드 생산소 선택 → 건설.
- 골드가 30 차감되고 슬롯에 건물 이름이 표시된다. 같은 슬롯을 누르면 해체할 수 있다.
- 지원 건물은 골드 20 + 보석 2를 사용하는 **검증용 데이터**다. 보석이 부족하면 실행할 수 없다.
- 데이터/단순 시각 프리팹: `Assets/Data/UI/BuildingIntegration/`. 이 수치는 정식 밸런스가 아니다.
- 백엔드 건설과 재화 처리는 실제 코드지만, 전장 배경·전투 입력은 기존 UI 프리뷰다. 실제 팀 맵/유닛 스폰이 통합된 완성 게임 씬은 아니다.
- 사용자의 열린 Unity 창이나 씬을 조작하지 않았다. 원하는 시점에 직접 씬을 열어 확인한다.

다른 씬에서는 `RuntimeBuildingUiBinding`에 카탈로그·정보·행동 패널과 **같은** BuildingBuildController / RunCurrencyManager / GameFlowController를 연결해야 한다. `RuntimeBuildingSlotButton`은 지도 버튼과 실제 슬롯을 연결한다. 기존 임시 메뉴와 UI가 입력을 동시에 소유하지 않도록 협의해야 한다. 새 검증 씬에서는 컨트롤러의 `slotMask = 0`으로 임시 월드 클릭 경로만 차단했다.

## 남은 범위와 담당자 확인

- 업그레이드 API가 없으므로 강화 항목은 숨겼다. `TryReplace`는 업그레이드가 아니며 호출하지 않는다.
- 건설 공간 해금 상태/해금 선택 API는 별도다. 현재 비활성 슬롯/콜라이더는 선택·실행하지 않는다.
- 실제 유닛 생산 건물/아군 스폰과 팀 전투 맵 연결은 미완료다. 생산 모듈 부착은 확인했지만 병영→실제 AI 전투는 검증하지 않았다.
- Core는 Finished 단계만 공개하고 최종 ResultType을 외부에 전달하지 않는다. HasClearedMainGame으로 최종 승리를 추정하지 않았다. 최종 결과 이벤트·정산 데이터 계약이 필요하다.
- 원본 건설 API에는 일부 실패 경로의 차감 롤백이 아직 보장되지 않는다. UI는 중복 재화 항목·잘못된 데이터 실행을 막고 실패를 자동 재시도하지 않지만, 원본 거래의 원자성을 대신 보장하지 않는다.
- 상점, 전체 분기 콘텐츠, 최종 승패 UI 자동 연결은 이번 완료 범위가 아니다.

## 검증

Unity `6000.3.23f1`의 별도 복사본 `D:/UnityUIValidation/OZGL2_2-player-ui-20260914`에서 수행했다. 사용자 프로젝트를 직접 Play하거나 저장하지 않았다.

- 런타임·Editor 컴파일 성공.
- 건설/두 재화/환급/중복 클릭/조건 재검사/페이즈 차단/해체 후 슬롯 표시 검사를 실제 Play Mode로 실행해 **112개 assertion이 통과**했다. 최종 실행에서 UI/게임 오류 및 예외는 0건이다.
- 실제 유닛 정보 회귀 검사 83개 통과.
- 기존 플레이어 UI 검사 263개와 실제 유물 보상 검사 129개 통과.
- 기존 실제 유닛 선택 검사 135개와 생존 수 검사 58개 통과.
- 1280×720 건설 전/후/두 재화 표시 화면 3장을 실제 렌더로 확인했다. 기존 UI 회귀는 1920×1080 및 1280×720도 검사했다.
- Unity Editor Search 인덱싱의 기존 시작 예외는 별도 기록했다. Core CS1998 경고는 원본 그대로 보존했다.
- 플랫폼 Player 빌드, 실제 팀 맵 전체 플레이, 물리 키보드 포커스 전달은 미검증이다.

최종 건설 검사에서는 Editor Search 시작 예외도 0건이었다. 기존 플레이어 UI 회귀 실행에서는 같은 기존 Search 예외 1건을 별도로 분류했다.

전체 근거 및 실패한 중간 로그도 `Logs/UiBuildingIntegration20260915/`에 보관한다. 최종 요약/화면은 `FinalResults/`에 있다. 로그는 Git 제외 대상이다. 검증된 새 씬·데이터·메타파일 16개만 작업 프로젝트에 전달했으며, 기존 씬/프리팹은 저장하거나 덮어쓰지 않았다.
